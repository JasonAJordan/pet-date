   <div class="logo-banner">
    <h1><img src="https://www.freelogodesign.org/file/app/client/thumb/3fbf174c-c555-4d1c-b145-f1099de24191_200x200.png?1607351240389"> </h1>
    <h3> Why Should Your Pet's Only Bestfriend be You? </h3>
    </div>
    
<div class="show-container">
<div class="show-title">
<p> <%= @meeting.status %>: 
    <%= link_to @meeting.requester.name, pet_path(@meeting.requester.id) %> and 
    <%= link_to @meeting.requestie.name, pet_path(@meeting.requestie.id) %> at 
    <%= link_to @meeting.location.name, location_path(@meeting.location.id) %>: 
    <%= @meeting.time_nice %>
    
    <%# @meeting.current_date %>
</p>

<p> <%= @meeting.has_passed %> </p>
   

<label> <h3>Your Buddy </h3></label> 
   
   <% pic = @meeting.requestie.picture  %>
   <% variant = pic.variant(resize_to_limit: [300, 300]).processed %>
    <img src="<%=(url_for(variant))%>">


<label><h3> Their New Buddy</h3> </label> 
   
   <% pic = @meeting.requester.picture %>
   <% variant = pic.variant(resize_to_limit: [300, 300]).processed %>
    <img src="<%=(url_for(variant))%>">


<label><h3> At <%= @meeting.location.name %></h3> </label> 
<% if @meeting.location.picture.attached? %>
   <% pic = @meeting.location.picture %>
   <% variant = pic.variant(resize_to_limit: [300, 300]).processed %>
    <img src="<%=(url_for(variant))%>">
<% end %>

<h3> <%= link_to "Back to all meetings", meetings_path %> </h3>

<br> <br>
 <% if @current_user.id == @meeting.requester.user.id  || @meeting.requestie.user.id %>  
 <%= button_to "Delete Date", @meeting, :method => :delete %>
<% end %> 
</div>
</div>