<% if flash[:errors] %>
  <ul>
  <% flash[:errors].each do |error| %>
    <li><%= error %></li>
  <% end %>
  </ul>
<% end %>

<%= form_for @location do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br><br>

  <%= f.submit %>
<% end %>